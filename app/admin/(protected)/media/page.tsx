export default function MediaPage() { return <div className="p-5 md:p-10"><p className="text-[10px] uppercase tracking-editorial text-muted">Library</p><h1 className="mt-3 font-display text-6xl tracking-[-0.05em]">Media</h1><div className="mt-14 max-w-2xl border-t border-line pt-5 text-sm leading-7 text-muted"><p>Media is organized inside each project so upload, ordering, cover selection, and image context stay together. The uploader sends originals directly to MinIO, then creates responsive WebP variants with Sharp.</p></div></div>; }
